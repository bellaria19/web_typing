import { RestartButton, RestartIcon } from "@/styles/typing.styles";

const RestartBtn = () => {
  return (
    <RestartButton>
      <RestartIcon />
    </RestartButton>
  );
};

export default RestartBtn;
